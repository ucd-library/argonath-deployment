apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  # direct access to the pod from within the cluster, no load balancing
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432
